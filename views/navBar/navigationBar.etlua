<!-- Navigation -->
<nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
    <!-- THE HEADER -->
    <% render("views.navBar.navBarTitle") %>

    <!-- TOP MENU -->
    <% render("views.navBar.topMenu") %>

    <!-- SIDE MENU -->
    <% render("views.navBar.sideMenu") %>
</nav>